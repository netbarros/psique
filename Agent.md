# Agent.md — Compatibility Bridge

This file exists for tooling/IDE compatibility.

## Canonical Source
Use [`AGENTS.md`](./AGENTS.md) as the single source of truth for multi-AI behavior.

## Rule
Do not duplicate or redefine governance rules here. If a conflict appears, `AGENTS.md` wins.
`docs/stitch/*` is canonical. `files/*` is generated mirror only (no manual edits).

## EN
Compatibility pointer only. Read `AGENTS.md` for all operational rules.
